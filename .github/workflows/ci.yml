name: CI - Build and Push Docker Images

on:
  push:
    branches:
      - "**" # Run on all branches

jobs:
  build-frontend:
    name: Build and Push Frontend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push frontend image
        uses: docker/build-push-action@v4
        with:
          context: ./frontend # Correct
          file: ./frontend/Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/nexskill-frontend:latest

  # --- THIS IS THE CORRECTED JOB ---
  build-link-service:
    name: Build and Push Link-Service
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push link-service image
        uses: docker/build-push-action@v4
        with:
          # It now points to your real service directory
          context: ./link-service
          file: ./link-service/Dockerfile
          push: true
          # It now builds the correct image name
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/nexskill-link-service:latest

  build-nginx:
    name: Build and Push Nginx
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push nginx image
        uses: docker/build-push-action@v4
        with:
          context: ./nginx # Correct
          file: ./nginx/Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/nexskill-nginx:latest
